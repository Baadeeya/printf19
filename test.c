#include <stdio.h>
#include <stdlib.h>
#include <strings.h>

int	main(void)
{
	int 	i = 0;
	char	str[] = "salut";
	char	*ptr;

//	ptr = malloc(sizeof(char *) * 40);
	printf("%s", ptr);
	free(ptr);

//	i = (printf("%y lol", "r"));
//	printf("%d", i);
	return (0);
}
